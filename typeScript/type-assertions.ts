let message ;

message = ' Hello world';

let count = (<string>message).length; //any olarak tanımlanan değisken parantez ve küçük büyük işaretleriyle belirlenen türde olduğu belirtilebilir.
let length = (message as string).length; //message değişkeni string türüne dönüştürldü


